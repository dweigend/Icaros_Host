{
	"name": "icaros-host",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"start": "bun server/index.ts",
		"start:strict": "bun server/index.ts",
		"dev": "echo 'Use bun start for the HTTPS Host. Use bun run dev:ui-only only for isolated Svelte UI work.' && exit 1",
		"dev:ui-only": "vite dev --host 127.0.0.1",
		"dev:lan": "echo 'LAN dev without HTTPS is disabled. Use bun start.' && exit 1",
		"build": "vite build",
		"preview": "echo 'Use bun start to preview the HTTPS Host.' && exit 1",
		"serve": "bun server/index.ts",
		"serve:lan": "PORT=5183 HOST=0.0.0.0 bun server/index.ts",
		"hardware:host": "bun start",
		"serve:e2e": "PORT=5183 HOST=0.0.0.0 bun server/index.ts",
		"firmware:m5:build": "uvx --from platformio platformio run -d firmware/m5-controller",
		"m5:pairing": "bun scripts/m5-pairing-cli.ts",
		"smoke:runtime": "bun scripts/smoke-runtime.ts",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"lint": "biome check .",
		"format": "biome check --write .",
		"test": "vitest run"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"@sveltejs/adapter-node": "^5.5.4",
		"@sveltejs/kit": "^2.57.0",
		"@sveltejs/vite-plugin-svelte": "^7.0.0",
		"@types/ws": "^8.18.1",
		"svelte": "^5.55.2",
		"svelte-check": "^4.4.6",
		"typescript": "^6.0.2",
		"vite": "^8.0.7",
		"vitest": "^4.1.8"
	},
	"dependencies": {
		"@lucide/svelte": "^1.17.0",
		"bits-ui": "^2.18.1",
		"ws": "^8.21.0"
	}
}
