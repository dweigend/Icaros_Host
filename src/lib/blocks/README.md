# blocks

Purpose: composed Icaros Host UI surfaces built from local primitive families.

Blocks may compose primitives from `$lib/components`, but public imports should
flow through this layer or a family `index.ts`.
