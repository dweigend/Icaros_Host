# select

Purpose: minimal Bits UI Select wrapper for Host console option lists.

This family keeps the official Bits UI compound structure and exposes only the
single-select API currently needed by Launch Selection.
