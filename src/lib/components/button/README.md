# button

Purpose: local button primitive for the Icaros terminal console.

This family wraps Bits UI `Button.Root` so links and form buttons keep the
official Bits behavior while Icaros owns only the terminal styling variants.
