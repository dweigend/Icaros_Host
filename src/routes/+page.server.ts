/**
 * Purpose: route entrypoint for the single Icaros Host console server contract.
 * Implementation stays in the private route-local console server module.
 */
export { actions, load } from './_console/server/page-server';
